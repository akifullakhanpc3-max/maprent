import AdminLogin from "@/views/admin/AdminLogin";

export const metadata = {
  title: "Admin Login | Occupra",
  description: "Platform Security Console — encrypted admin authentication",
};

export default function AdminLoginPage() {
  return <AdminLogin />;
}
